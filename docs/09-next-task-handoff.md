# 다음 작업 인수인계 (HANDOFF)

> **이 문서는 "바로 다음에 할 작업 한 건"만 담는다.** 작업이 끝나면 내용을 통째로 다음 작업으로 갈아끼운다.
> 과거 기록은 [07 작업로그](07-work-log.md) §4로 간다.
>
> **읽는 순서**: [07](07-work-log.md) §1(현재 상태) → §2(개발 재개 절차) → 이 문서.

- **작성**: 2026-09-06
- **직전 작업**: ✅ **받은 신청 14일 무응답 만료 + 신고·차단 정리 범위 확대**(**V24**)
- **다음 작업**: 남은 것은 **막힌 것 3건**(§2) — C(로그인 배경 그림) · D·E(외부 키).
  기획 답변이 더 오지 않으면 **§4 출시 준비**(일본어 검수가 블로커)로 간다

> 📌 **다른 기기에서 이어받는다면**: `git pull` → [07 §2-2](07-work-log.md) 루틴 → 아래 §6.
> **V24가 들어왔다**(`chat_requests.status`에 `EXPIRED`). 서버를 한 번 띄워 적용할 것.
>
> 🚨 **기획서 정본이 `달빛톡 기획서_260906.docx`다**(Plan_4 폴더, 파일명이 `수정2`에서 바뀌었다).
> 이 판에서 6-1·6-2가 늘어났고 전부 반영됐다. **다음에 기획서를 받으면 본문을 옛 판과 통째로
> diff할 것** — 화면만 보면 문장 몇 줄이 조용히 늘어난 것을 놓친다(실제로 놓쳤다).

---

## 1. 지금 무슨 상황인가

**Plan_3의 ①~⑦단계와 기획서 화면 31장 대조가 모두 끝났다.**
그 뒤로 **막혀 있던 기획 답변 둘(A·B)이 풀려** 둘 다 구현됐고,
**기획서 260906판에서 늘어난 6-1·6-2 규칙까지 반영**했다.

| 단계 | 내용 | 마이그레이션 |
|---|---|---|
| ① | 게이트 폐지 + 영업일 KST 18시 | V10 |
| ② | 포스트 — 메인 사진 · 9장 · 삭제 3회 | V11 |
| ③ | 달빛가든 — 열람 제한 · 노출 알고리즘 | V12 |
| ④ | 댓글 — 3단계 · 이미지 · 50자 | V13 |
| ⑤ | 달빛 한마디 | V14 |
| ⑥ | [포스트 정보] + 대화방·친구 개편 | V15~V18 |
| ⑥-B | 남은 화면 맞춤 — **31장 전부 대조** | V19 |
| ⑦ | **BM·쿼터 마무리** | V20~V22 |
| — | Plan_4 리소스 46장 적용(포스트·달빛가든·하단 주메뉴) | 없음 |
| A | 부스트 최우선권(B3) + SPOTLIGHT_BOOST 정리 | **V23** |
| B | **대화방 종료 규칙**(30일 무대화 → `ENDED`) | 없음 |
| — | **받은 신청 14일 무응답 만료**(대화·친구) + 신고·차단이 신청까지 정리 + **차단이 상대 가든에서도 즉시 반영** | **V24** |

---

## 2. 🚧 막혀 있는 것 셋 (다음에 할 일은 여기서 나온다)

### 2-1. 기획 답변을 기다리는 것 하나 (A·B는 해소됐다)

| # | 무엇 | 왜 막혔나 |
|---|---|---|
| ~~A~~ | ~~부스트 "3회 노출 우선권"~~ | ✅ **2026-09-06 답변 받아 구현 완료**(V23) |
| ~~B~~ | ~~매칭 대화방 종료 규칙~~ | ✅ **2026-09-06 답변 받아 구현 완료**. 아래 §3-0 참고 |
| C | **로그인 배경 그림 교체** | `login_bg.jpg`에 *"밤에만 만나는 대화"* 가 구워져 있는데 야간 게이트는 **폐지**됐다. 코드로 못 고친다. (빈 상태 배경은 2026-09-06에 한국어판을 받아 **해소**됐다 — 언어별 그림 규칙은 [08](08-assets-checklist.md)) |

**곁들여 물어보면 좋은 것**
- 🆕 **받은 신청 카드의 주황 `N`** — 기획서는 *"포스트 정보창을 호출하지 않은 목록"* 에만 붙이라는데,
  지금은 **대기 중이면 무조건** 붙는다. 열람 여부를 담을 칼럼이 없어 구분을 못 한다(칼럼 하나면 된다)
- 🆕 **친구 신청 만료 기간** — 기획서는 **대화 신청**의 14일만 말한다. 친구 쪽도 같게 뒀는데
  (`app.friend.request-expire-days`), 다르게 가려면 이 값만 바꾸면 된다
- 친구 카드 도시 표기 — 시안은 소문자 영문(`seoul`), 지금은 각 언어 도시명("서울")
- 프라임 구독 중에도 요금제 목록을 보여 줄지(시안 img26 우측은 보여 준다)
- 댓글창 무료 5회의 **초기화 주기** — 음성에만 *"초기화 되지 않음"* 이 적혀 있어
  댓글은 **영업일마다 초기화**로 읽었다. 평생 5회가 맞다면 `lifetime_usage`로 옮기면 된다

### 2-2. 외부 키가 있어야 하는 것 둘

| # | 무엇 | 지금 상태 |
|---|---|---|
| D | **번역 공급자**(Google/DeepL 등) | 인터페이스·쿼터·버튼이 전부 준비돼 있다. `TranslationProvider` 구현 하나 + `app.translate.provider` 설정이면 켜진다 |
| E | **인앱결제 스토어 계정** | 결제 검증(`/store/purchases:verify`)은 붙어 있고 실제 결제창만 없다(01 §1.8 ①) |

> 📌 **D는 켜기 전까지 아무것도 소모하지 않는다.** `provider: none`이면 무료 자리를
> 쓰지 않도록 해 두었다 — 번역이 안 되는데 횟수만 깎이면 사용자는 잃기만 한다.
> 개발 중 흐름을 보려면 `--app.translate.provider=mock`(⚠️ 운영 금지).

---

## 3. 최근에 얻은 것 (다음 사람이 알아야 할 것)

### 3-0. 대화방 종료 규칙 — 2026-09-06 구현 (B 해소)

**규칙**: 방 안에 채팅 로그가 하나도 남지 않으면 그 방은 사라진다.
메시지가 30일 뒤 사라지므로 결국 **30일간 무대화면 종료**다.

| 항목 | 확정 |
|---|---|
| 기준 | **마지막 메시지 시각(없으면 방 생성 시각)** 이 30일보다 오래되면 |
| 친구 방 | **구분 없다.** 친구 방도 30일 — `retention-days-friend`(365) 자체를 없앴다 |
| 삭제 방식 | row 삭제가 아니라 **`ENDED`** + `active_pair_key` 비우기 |
| 다시 대화 | 새 방을 만든다(`POST /chat/rooms:with/{userId}`) |
| 무료 번역 자리 | 🚨 **돌아오지 않는다.** 평생 5개 |

🚨 **"갓 만든 방"과 "로그가 다 사라진 방"은 DB에서 구분되지 않는다** — 둘 다 0건이다.
그래서 `COUNT(*) = 0`으로 찾으면 **방을 만들자마자 닫힌다.**
마지막 활동 시각을 기준으로 삼으면 기획이 말한 "처음 만들어질 때는 예외"가 규칙 안에 들어간다.

🚨 **친구 방을 닫으면 친구인데 대화를 못 걸 수 있었다.** 방을 만드는 곳이
**친구 수락 시점 하나뿐**이라, 한 번 닫히면 친구 목록의 `roomId`가 계속 비어
[대화하기]가 아무 반응도 안 했다. `ChatService.ensureFriendRoom()`을 새로 두고
[포스트 정보]의 [대화하기]가 부르게 했다. **친구가 아니면 만들지 않는다** —
모르는 사이의 방은 대화 신청 수락으로만 생겨야 신청 절차와 루나 비용을 건너뛰지 않는다.

🚨 **무료 번역 자리가 풀리고 있었다.** `countActiveTranslateRooms`가 `status='ACTIVE'`로
세고 있어 방이 닫히면 자리가 돌아왔다 — 방을 비우기만 하면 무한히 쓸 수 있었다.
방 상태를 보지 않고 **켜 둔 이력의 수**를 센다(그래서 `translate_rooms`의 행은 지우지 않는다).

**확인은 배치를 수동으로 돌려서** — `POST /internal/scheduler/purge-messages`
(메시지 삭제 → 방 종료를 **실제 잡과 같은 순서로** 부른다. 순서가 틀리면 방이 안 닫힌다).

### 3-0-B. 신청 만료·신고 차단에서 얻은 것 — 2026-09-06 (**V24**)

#### 🚨 상태값을 재활용하기 전에 **거기 붙어 있는 부수효과**를 셀 것

받은 신청 14일 만료를 `REJECTED`로 두려다 멈췄다. 거절에는 **1일 재신청 금지**(V18)가 딸려 있어서,
**내가 답을 안 했을 뿐인데 상대가 다시 신청도 못 하게 된다.** 이름이 비슷하다고 뜻이 같지 않다 —
`EXPIRED`를 새로 만들었다.

#### 같은 규칙이라도 **표의 유니크 제약**이 처리를 가른다

| | 대화 신청 | 친구 신청 |
|---|---|---|
| 만료 처리 | `status='EXPIRED'`(행 유지) | **행 삭제** |
| 왜 | 목록이 전부 `PENDING` 조회라 상태만 바꿔도 사라진다 | `pair_key`가 UNIQUE — 남기면 **다시 신청 못 한다** |

#### 🚨 **캐시해 둔 순서로 그린다면, 그리는 시점에도 자격을 다시 볼 것**

달빛가든이 차단을 **후보 산정 질의**에서만 걸렀다. 순서는 진입 시 한 번 정해지고 스크롤은 전부
`selectCandidatesByIds`를 타는데 거기엔 조건이 없어서, **차단당한 쪽 화면에 차단한 사람이
계속 나왔다.** 옛 주석은 *"한 세션이면 지나간다"* 며 의식적으로 넘긴 것이었는데,
차단은 잠시 뒤에 반영되면 되는 일이 아니다.

#### 목록에서 빼는 것으로는 부족하다 — **상세도 막아야 한다**

낡은 목록·이미 떠 있는 화면이 상세로 들어오는 통로가 된다. [포스트 정보]는 409로 막는다.

#### 📌 고친 뒤에는 **되돌려서 실패하는지** 볼 것

고친 코드로 통과해도 원래 통과했을 수 있다. 매퍼만 되돌려 서버를 다시 띄우고 **2페이지에
그대로 나오는 것**을 눈으로 본 뒤 복구했다. 이 왕복이 없으면 "고쳤다고 믿는다"가 된다.

### 3-1. Plan_4 리소스를 다룰 때 (2026-09-06)

🚨 **좌표 표기가 바뀌었다.** Plan_3은 `X*Y` 단위(×28.4), **Plan_4는 1080 캔버스 픽셀값 그대로**.
같은 폴더의 `*_참고용.png`가 **옛 표기**라 섞어 읽으면 28배 어긋난다 — `*_좌표값.png`가 정본.
공용 도구는 `shared/widgets/design_canvas.dart`(`DesignCanvas` + `ArtImage`).

- **세로 좌표는 그대로 쓰지 않는다.** 기기 높이가 달라 카드가 늘어난다 —
  **가로 위치·크기만** 시안을 따르고 세로는 카드 위/아래에 붙인다.
- 🚨 **시안 치수에는 예외 없이 `* DesignCanvas.scaleOf(context)`를 태울 것.**
  `AppDimens` 토큰(논리px)과 섞으면 **화면마다 크기가 갈라진다** — 실제로 같은 카드인데
  포스트와 달빛가든의 폭·높이가 달랐다(함정 #49).
- 🚨 **줄 높이는 그 줄에서 제일 큰 요소가 정한다.** 머리글을 타이틀(66~71)로 계산했는데
  실제로는 **Prime 버튼(83)** 이었고, 두 화면의 카드 시작이 7px 어긋났다(함정 #50).
- **두 화면의 카드는 같은 크기여야 한다** — 시안에서 둘 다 `23, 475`에 1032×1797이다.
  위 높이 계산은 **`DesignCanvas.aboveCard()` 한 함수**로 합쳐 뒀다. 새로 만들지 말 것.
- **외곽선 PNG는 여백이 제각각이다.** 알파를 실측해서 맞춰야 한다(함정 #51).
  사진은 **선 두께만큼 안으로 밀어 넣어** 구조적으로 못 나가게 한다(#52) —
  곡률만 맞추면 기기 비율이 달라질 때 또 삐져나온다.
- **그림이 메시지를 갖고 있으면 글자를 또 그리지 말 것.** 빈 상태 배경에 안내가 구워져 있는데
  그 위에 ARB 문구를 얹었다가 **같은 말이 두 번** 나왔다.
- **언어별 그림은 `DesignCanvas.localizedAsset()`.** 원문(한국어)은 원래 자리,
  일본어판은 **같은 파일명으로 `ja/` 하위 폴더**. 두 벌이 다 있는 그림에만 쓸 것.
- 새로 겪은 레이아웃 함정 둘: **`Center`는 `heightFactor` 없으면 최대 높이까지 늘어난다**(#46),
  **`Stack`은 기본이 loose라 스크롤뷰가 내용만큼 줄어든다**(#47). 둘 다 오류가 안 난다.

> 📌 **눈대중으로 맞추려 하지 말 것.** "조금 다른 것 같다"가 세 번 나왔고 매번 원인이 달랐다.
> 화면 픽셀과 PNG 알파를 **직접 재면**(PowerShell + .NET `System.Drawing`) 몇 분이면 나온다.
> ⚠️ 그 스크립트는 **영어로** 쓸 것 — 한글이 들어가면 PowerShell이 파싱을 못 한다(함정 #53).

### 3-2. 부스트 최우선권 — 2026-09-06 구현 (A 해소)

**"구매 즉시"가 아니라 "사용했을 때"** 작동한다. 부스트 풀 **안쪽 정렬**에만 얹었고
6:4 믹싱은 건드리지 않았다 — 우선권은 **부스트 사용자끼리** 겨룰 때의 문제다.

🚨 **세는 단위가 "노출 횟수"가 아니라 "본 사람 수"다.** 한 사람에게 세 번 보여도 1이다.
새 테이블 없이 `feed_exposures`를 세는데, 이 표가 `(본 사람, 보여진 사람)` PK로
**한 사람당 한 행**이라 행 수가 곧 사람 수가 된다. 규칙이 자료구조와 맞아떨어진 경우다.

`app.garden.boost.priority-viewers`(기본 3). ⚠️ **이 값은 체감상 거의 즉시 사라진다** —
서버는 카드를 목록에 실어 보낸 것까지만 알고 스크롤 여부는 모르기 때문이다.
혜택으로 느껴지지 않으면 값을 올릴 것(재배포 불필요).

**검증 방법도 남겨 둔다** — 순서가 그럴듯한 것으로는 확인이 안 된다.
**점수가 낮은 쪽에만 우선권을 주고** 설정을 켰다 껐다 하며 순서가 뒤집히는지 봐야 한다.

### 3-3. ⑦단계에서 얻은 것

### 🚨 값이 아니라 **"무엇을 세는가"** 를 먼저 볼 것

무료 번역이 기획서와 달랐는데, **숫자만 다른 게 아니었다.**

| 자리 | 기획서 | ⑦ 전 |
|---|---|---|
| 댓글창 | "댓글창 **5회 호출**" | 번역 **건수** 2 |
| 대화방 | "**5개까지**, 삭제 전까지" | 하루 **상대 수** 2 |

`2 → 5`만 고쳤다면 여전히 틀렸다. 댓글은 **창**이 단위라 창 하나에 댓글이 열 개여도
자리는 하나다. 대화방은 날짜가 아니라 **동시에 열어 둔 방의 수**다.

### 🚨 못 주는 것은 **받지도 말 것**

번역 공급자가 없으면 자리를 쓰지 않는다. 기능이 준비되지 않았을 때
**아무것도 소모하지 않는 것**이 옳은 기본값이다.

### 기획서가 허락한 삭제는 삭제해도 된다

GIF는 *"문제 있을 경우 **해당 기능 삭제**"* 라고 적혀 있었다. 키 없이 죽은 버튼을
남기는 것보다 낫다.

### 🐛 COALESCE는 **바깥**에

`SELECT COALESCE(likes, 0) FROM post_stats WHERE ...` — 컬럼 NULL만 막고
**행이 없으면** 0행 → null → int로 받다가 터진다. `SELECT COALESCE((SELECT ...), 0)`.
통계 행은 첫 노출 때 생기므로 **공유 직후에는 늘 없는 상태**다.

---

## 4. 앞으로 할 만한 것 (막힌 것 말고)

기획 반영이 끝났으니 이제는 **출시 준비**다.

1. **일본어 검수** — ARB가 두 언어 다 채워져 있지만 **기계 번역 수준**이다.
   한·일 동시 오픈이 확정이고 일본어가 출시 블로커다(2026-08-08 기획 미팅).
2. **성능·부채** — [07 §5](07-work-log.md) 참고. 피드 후보 쿼리에 LIMIT이 없다(§5-1).
3. **운영 준비** — 신고 처리 도구, 배치 모니터링, 오류 알림.
4. **친구 기획** — 미팅에서 *"아직 논의 전"* 으로 동결돼 있다.

---

## 5. 작업하기 전에 꼭 읽을 것

**[12 §6](12-plan3-diff.md)의 원칙 둘**

1. **기획서에 없으면 사라진 것으로 본다.** 재활용은 **규칙이 실제로 같을 때만**.
2. 🚨 **콘텐츠 이름은 언제든 바뀐다** — **이름을 식별자로 쓰지 말 것.**

**단계마다 얻은 것**

- ② **숫자를 문구에 굳히지 말 것.** 한도는 서버가 `field`로 싣고 ARB가 조립한다.
- ③ 🚨 **제한은 화면이 아니라 서버가 건다.**
- ③ **삭제와 빈자리는 다르다.**
- ③ 겹쳐 놓는 **장식 레이어에는 `IgnorePointer`**(함정 #38).
- 📌 **사진을 누르는 자리는 `TappableImage`**(`shared/widgets/image_viewer.dart`).
- ⑤ **여러 조건은 한 문장으로 줄여 볼 것.**
- ⑥ **화면 상태를 목록에서 들고 오지 말 것.** 목록은 낡는다.
- ⑥ **우리가 지어낸 문구를 상대의 말인 척 두지 말 것.**
- ⑥-B 🚨 **화면이 숫자를 말해야 하면 서버가 그 숫자를 줘야 한다.**
- ⑥-B 🚨 **혜택 목록은 권리에서 나와야 한다** — 코드가 따로 봐주는 혜택은 화면에 안 뜬다.
- ⑦ 🚨 **값이 아니라 "무엇을 세는가"를 먼저 볼 것**(§3).
- ⑦ 🚨 **못 주는 것은 받지도 말 것**(§3).
- 🚨 **상태값을 재활용하기 전에 부수효과를 셀 것**(§3-0-B).
- 🚨 **캐시해 둔 순서로 그린다면 그리는 시점에도 자격을 다시 볼 것**(§3-0-B).
- 📌 **고친 뒤에는 되돌려서 실패하는지 볼 것**(§3-0-B).

---

## 6. 개발 환경 시작

```bash
git pull origin main
```

[07](07-work-log.md) §2-2 루틴대로: MariaDB 기동 → 서버 `bootRun` → `adb reverse tcp:8080 tcp:8080` → `flutter run`.

| 잊으면 깨지는 것 | 왜 |
|---|---|
| 서버를 한 번 띄워 **V24까지 적용** | 번역 쿼터 개편(V20)·평생 사용량(V21)·신청 우선권(V22)·SPOTLIGHT enum 정리(V23)·**신청 만료 EXPIRED(V24)** |
| ⚠️ 대화방 종료 규칙(30일) | 30일 무대화 방이 배치에서 ENDED가 된다. 오래된 개발 데이터가 한 번에 닫힐 수 있다 |
| ⚠️ **받은 신청 만료 규칙(14일)** | 14일 넘게 답하지 않은 신청이 `expire-requests` 한 번에 전부 사라진다. **배치는 내 계정만 보지 않는다** |
| `flutter pub get` → **`flutter gen-l10n`** | ARB 생성물이 저장소에 있다(함정 #28) |
| `assets/` 폴더가 늘면 **완전 재빌드** | 번들에 안 들어간다(함정 #30) |

- **실기기**: `--dart-define=API_BASE_URL=http://localhost:8080` 필수(함정 #29)
- **에뮬 2대**: `Pixel_10` + **`Pixel_B`**(함정 #32)
- 📌 **검증 스크립트가 이제 저장소에 있다 — [`tools/`](../tools/README.md).**
  ```bash
  python tools/verify/verify_moderation.py        # 신청 만료 · 신고/차단 정리 범위
  python tools/verify/verify_feed_block_page2.py  # 차단이 캐시된 순서에도 먹는가
  python tools/spec/diff_docx_text.py "<옛 기획서>" "<새 기획서>"
  ```
  `_common.py`의 `login()`·`publish_post()`·`clear_ties()`로 **새 검증도 몇 줄이면 쓴다.**
  (예전엔 `scratchpad/`에 두고 커밋하지 않아 다른 기기에서는 **문서만 있고 실물이 없었다.**)
- **18시가 지나면 가든이 빈다** — 오늘 공유한 사람이 아직 없어서다(버그가 아니다).
  `publish_post()`가 하는 일이 이것이고, 직접 하려면 이렇다:
  ```sql
  INSERT INTO posts (id, user_id, session_date, published_at, content_updated_at)
  VALUES ('p-x', '<userId>', '<영업일>', NOW(), NOW())
  ON DUPLICATE KEY UPDATE published_at = NOW(), content_updated_at = NOW();
  ```
  본인은 후보에서 빠지고, **15분 안에 이미 본 상대도 빠진다** —
  다시 보려면 `DELETE FROM feed_exposures WHERE user_id='<보는 사람>';`
- **번역 흐름을 보려면** `./gradlew bootRun --args='--app.translate.provider=mock'`
  (⚠️ 운영 금지 — 번역하지 않고 `〔ja〕` 표시만 붙인다)
- **가든/포스트 정보를 확인할 땐** 무료 계정으로 — 앨범패스·프라임은 열람 제한이 안 걸린다
- **`last_seen_at`은 소켓이 붙어야** 채워진다. REST만 두드리면 null인 게 정상이다
- **MariaDB를 내릴 땐** `mysqladmin -uroot shutdown`

---

**작업이 끝나면**: 이 문서를 다음 작업으로 갈아끼우고, [07](07-work-log.md) §4에 세션 로그를 추가하고,
[04](04-progress-and-roadmap.md) 체크박스를 갱신할 것.
