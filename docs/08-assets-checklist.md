# 달빛톡 — 리소스(이미지·아이콘) 체크리스트

> **이 문서의 용도**: 디자이너/기획자에게 그대로 전달할 수 있는 **필요 리소스 목록과 규격**.
> 현재 앱에 들어 있는 이미지는 기획서에 딸려온 **임시 더미 4장**뿐이고, 나머지는 전부 미제작 상태다.
> 파일이 준비되면 이 문서의 체크박스를 채우고 [04 로드맵](04-progress-and-roadmap.md)에도 반영할 것.

---

> 🚨 **UI를 건드리기 전에 [14 UI 작업 대전제](14-ui-ground-rules.md)를 먼저 읽을 것**(2026-09-14 확정).
> 요지: 언어는 **한국어·일본어 둘뿐** · **그림에 글자가 붙어 있으면 그림 자체가 언어별로 바뀐다**
> · 폰트가 그리는 글자는 **상태·수치 표시**가 기본.

## 0-1. 🇯🇵 일본어판 현황 (2026-09-14 전달본 반영)

✅ **하단 주메뉴 글자 10장을 받았다.** 이제 일본어로 켜면 메뉴가 「投稿」「月光ガーデン」…로 바뀐다.
빈 상태 배경까지 **11장이 두 벌**을 갖췄고, 목록은 `DesignCanvas.localizedAssets`가 정본이다.

🚨 **받은 일본어 글자 두 장이 탭 한 칸(≈196px)보다 넓다** — 「月光ガーデン」 232 ·「プロフィール」 234.
그대로 두면 옆 칸과 겹치고 오른쪽이 잘려서(실제로 그랬다) **비율 그대로 84%로 줄여 그리고 있다.**

📌 **다시 받을 필요는 없다.** 잘리지도 겹치지도 찌그러지지도 않는다. 남는 것은 미관 하나 —
일본어 라벨이 한국어보다 약 15% 작게 보인다(글자 높이 40 → 34).
⚠️ **"한국어와 같은 폭(146·107)으로" 요청하면 더 나빠진다** — 그 폭이 더 좁다.
글자를 키우려면 **규격이 아니라 문구**를 줄여야 한다(「ガーデン」·「プロフ」).
일본어 원어민 검수 때 **라벨 길이를 함께 정하는 것**이 한 번에 끝내는 길이다.

### 아직 일본어판이 없는 그림 (10장)

목록에 없으면 **한국어가 그대로 나간다**(깨지지 않는다). 받는 대로 켜진다.

| # | 파일 | 박힌 글자 | 규격 |
|---|---|---|---|
| 1 | `scene_post/title_post.png` | 포스트 | 232×66 |
| 2 | `scene_garden/title_garden.png` | 달빛가든 | 278×71 |
| 3 | `scene_garden/filtering_gender.png` | 성별 | 217×92 |
| 4 | `scene_garden/filtering_age.png` | 나이 | 227×94 |
| 5 | `scene_garden/filtering_nation.png` | 국가 | 217×94 |
| 6 | `scene_garden/button_Moonlight.png` | 달빛 한마디 | 335×96 |
| 7 | `scene_post/button_postalbum.png` | 포스트 앨범 | 601×96 |
| 8 | `scene_post/button_boost.png` | 부스트 | 407×96 |
| 9 | `scene_post/button_postmake.png` | 포스트 공유하기 | 418×103 |
| 10 | `scene_garden/button_addpost.png` | 새 사진 등록하기 | 499×116 |
| — | `login_bg.jpg` | *"밤에만 만나는 대화"* | 853×1200 | 🚨 **한국어판부터 다시** 받아야 한다 |

**필터 값 칩도 한국어다** — `여자`·`남자`·`10~40대`·`한국`·`일본` **9장**.
⚠️ 이건 `_kor`/`_jap` 접미사가 붙어 있지만 **UI 언어가 아니라 고른 나라**다. 헷갈리지 말 것.

⚠️ **관심사는 따로 크다 — 아래 §0-2를 볼 것.**

**받았을 때 할 일은 셋뿐이다**(호출부는 손대지 않는다):

1. `ja/`에 **같은 파일명**으로 넣는다
2. `DesignCanvas.localizedAssets`에 **원문 경로 한 줄**(규격이 다르면 일본어 규격도 함께)
3. 새 폴더였다면 `pubspec.yaml`에 `- .../ja/` 줄 추가 → `flutter clean` 후 재빌드

빠뜨린 것은 `test/localized_assets_test.dart`가 잡는다. 자세한 규칙은 [14 §2](14-ui-ground-rules.md).

## 0-2. 🚨 관심사 — 버튼·아이콘이 **전부 이미지**다 (기획 2026-09-19)

> **확정**: *"관심사에 존재하는 버튼들, 아이콘들은 전부 이미지 파일이야.
> 코드가 그려내거나 텍스트로 하는 게 아니야."*

**지금은 37종 중 `영화` 한 장만 와 있다**(`scene_garden/Interest_movie.png`, 204×89).
나머지 36종은 **코드가 `Icons.*`로 그리고 있다** — 임시이고, 리소스가 오면 갈아 끼운다.

🚨 **받은 한 장을 보면 요청 규모가 정해진다.** `Interest_movie.png`는
**아이콘과 `영화` 글자가 한 장에 같이 구워져 있다.** 글자가 들어 있으므로 [14 §2](14-ui-ground-rules.md)에 따라
**일본어판이 따로 필요하다** — 즉 칩만 **37 × 2 = 74장**이다.

| 무엇 | 개수 | 지금 | 비고 |
|---|---|---|---|
| 관심사 칩 (아이콘 + 라벨) | **37** × 2언어 = **74** | 1/37 (`영화`, 한국어만) | 규격은 받은 것과 같이 **204×89** |
| 관심사 **그룹** 아이콘 | 4 × ? | 0 — `Icons.*` | 취미 · 라이프스타일 · 문화/엔터 · 스포츠 |
| 시트 버튼 | — | 0 — `Icons.*` | `전체 초기화` · 칩의 `×` · 그룹 펼침/접힘 · `저장하기` |

**요청할 때 같이 정해야 하는 것** — 답이 없으면 두 번 받게 된다:

1. 🚨 **칩 한 장이 두 화면에 쓰인다.** 관심사는 **[관심사 등록] 시트**와
   **달빛가든 카드**(사진 위에 상대 관심사를 띄우는 자리, 기획 4-1) 양쪽에 나온다.
   **한 규격으로 둘 다 될지**, 아니면 자리마다 따로 받을지 확인할 것.
2. **선택/미선택 두 상태가 필요한지.** 시안 img21은 고른 칩에 **체크와 보라색 테두리**가 붙는다 —
   그림 두 벌(`_normal`/`_color`)로 올지, 그림 한 장 위에 코드가 표시를 얹을지.
3. **개수 표시(`(2/3)`)는 폰트가 맡는다**([14 §3](14-ui-ground-rules.md) — 값이 변하는 것).
   그림으로 만들지 말 것.

📌 **받으면 코드 쪽은 작다** — `ProfileCatalog`의 `IconData` 표를 **코드 → 에셋 경로**로 바꾸고
`ArtImage`로 그리면 된다. 달빛가든 쪽은 `garden_screen.dart`에 *"영화만 시안 그림이 있다"* 는
분기가 이미 있어 그 분기가 사라진다.

⚠️ **라벨이 그림에 들어가면 ARB의 `interest*` 37키가 남는다** — 카드 툴팁·접근성 등
그림이 못 가는 자리에 여전히 쓰이므로 **지우지 말 것.**

## 0. 현재 상태 (2026-09-06 갱신)

> 📁 **에셋 폴더는 전달 폴더를 그대로 따른다**(2026-09-14부터).
> `Plan_Chat/UI/Scene_Post` → `assets/images/scene_post/` · `Scene_Garden` → `scene_garden/`.
> **파일 이름도 전달본 그대로** 둔다 — 다음 전달본을 그대로 떨어뜨리면 되도록.
> 손댄 것은 둘뿐이다:
> - `frame_post_album Pass.png` → `frame_post_album_pass.png` (경로에 공백을 두지 않는다)
> - `*_jap.png` → **`ja/` 하위 폴더의 같은 이름** (우리 언어 규칙, [14 §2](14-ui-ground-rules.md))
>
> ⚠️ 전달본 이름이 늘 규칙적이지는 않다 — `menu__room_icon`(밑줄 둘) ·
> `main_menu_icon_friend`(접두어가 다름). **경로를 이름으로 조립하지 말 것**(조립하면 그 둘만 조용히 못 찾는다).

| 항목 | 상태 |
|---|---|
| 앱 내 이미지 | 로그인·온보딩 배경 **4장**(임시 더미) + **UI 전달본 69장**(2026-09-14 수령·적용) |
| 포스트·달빛가든 | ✅ **Plan_4 시안 전면 적용**(2026-09-06) — 배경·타이틀·버튼·필터·배지 |
| 하단 주메뉴 | ✅ **이미지로 교체**(2026-09-06) — 탭 5개 × 아이콘·글자 × 색상/흰색 20장 + 외곽선 |
| 관심사 | 🚨 **버튼·아이콘이 전부 이미지여야 한다**(기획 2026-09-19)인데 **`Interest_movie` 1장뿐**. 칩만 37×2언어 = **74장** + 그룹 아이콘·시트 버튼 → **§0-2** |
| 앱 아이콘 | ❌ **Flutter 기본 아이콘** 그대로 |
| 스플래시 | ❌ 기본 흰 화면 |
| 기능 아이콘 | 포스트·가든·주메뉴는 시안 적용. **나머지 화면은 아직 `Icons.*`** |
| 국기 | 이모지(🇰🇷🇯🇵)로 렌더링 — 기기·OS마다 모양이 다름 |
| 빈 상태 일러스트 | ❌ 없음(회색 아이콘 1개로 대체 중) |

> 2026-08-02에 쓰이지 않던 더미 6장(`avatar1~3`, `garden_sample`, `post_sample`, `profile_photo`)을 삭제했다.
> 정적 UI 시절 넣었던 것으로, 해당 화면들이 서버 실데이터로 전환되며 참조가 끊겼다.

### 🧭 리소스가 오기 전까지 UI를 만드는 방식 (2026-08-29 확인)

> 📌 **포스트·달빛가든·하단 주메뉴는 리소스를 받았다**(2026-09-06). 아래는 **아직 시안이 없는
> 나머지 화면**에 적용되는 이야기다. 시안이 있는 자리는 그림을 쓴다.

**UI 리소스는 기획/디자인 쪽에서 추후 별도로 전달된다.** 그때까지 이쪽에서 하는 일은 이렇다.

| 하는 것 | 안 하는 것 |
|---|---|
| 새 UI는 **코드로 그린다** — Flutter 위젯 + 테마 토큰(`AppColors`/`AppDimens`) + Material 기본 아이콘 | **이미지를 만들거나 어디서 따오지 않는다.** 캡처·생성·크롭 전부 해당 없음 |
| 전달받은 리소스가 있는 자리만 `ArtImage`로 그림을 쓴다 | 시안 없이 이미지를 임의로 채워 넣지 않는다 |

**왜 코드로 그리나** — 리소스가 오면 **그 자리만 갈아끼우면 되도록** 하기 위해서다.
색·여백·모서리를 테마 토큰으로 잡아 두면 그림으로 바뀔 때 레이아웃을 다시 짤 일이 없다.
반대로 임의 이미지를 채워 두면 최종본이 왔을 때 규격이 어긋나 두 번 일하게 된다.

> 🚨 **이건 "최종 모습"이 아니라 "버티는 방식"이다.** 화면이 멀쩡히 보여서 **완성된 자리로
> 읽히는 것이 이 방식의 함정**이다 — 실제로 관심사 시트를 보고 *"코드로 그리니 새 그림이
> 필요 없다"* 고 기록했다가 기획에게 정반대 답을 받았다(**버튼·아이콘은 전부 이미지**, §0-2).
> **리소스 상태는 화면이 아니라 이 문서의 목록으로 판단할 것**([07](07-work-log.md) 함정 #78).

> ⚠️ **얇은 선·단순 도형·라운드 사각은 앞으로도 이미지로 받지 말 것.**
> 늘리면 선 굵기와 곡률이 찌그러지고 번진다([07](07-work-log.md) 함정 #31).
> 실제로 `포스트 사진 외곽선.png`를 받았다가 쓰지 않고 `Border.all`로 코드로 그렸다.

#### 지금 코드로 그려 둔 것 — 시안이 오면 교체 대상

| 화면 | 요소 | 지금 모습 |
|---|---|---|
| ~~포스트(홈)~~ | ~~`[메인]` 배지·버튼~~ | ✅ Plan_4에서 **`[TOP]` 그림 두 벌**로 교체됨 |
| 포스트(홈) | **촬영 버튼**(무지개 링 + 흰 카메라) | Plan_4에도 그림이 없어 코드로 그림. 원형 그러데이션이라 코드가 더 깨끗하다 |
| ~~포스트·가든~~ | ~~카드 외곽선~~ | ✅ **그림을 쓴다**(2026-09-06). 실물로 보니 무지개 그러데이션이 코드보다 곱다 |
| 달빛가든 | **[사진 등록 안내창]**(③단계) | 카드 하단 패널. `Icons.lock_outline_rounded` + `FilledButton` |
| 달빛가든 | **잠긴 사진 인디케이터** | 상단 눈금 중 잠긴 칸을 흐리게(alpha 0.18). ⚠️ 시안은 `1/8` 같은 **숫자 표기**다 |
| 달빛가든 | **`[달빛 한마디]` 버튼**(필터 줄 4번째) | ✅ 채웠다(⑤단계). 달 아이콘 + 채워진 버튼 |
| **대화방·친구 상단** | **밤 풍경 배경**(⑥단계) | 밤하늘 그러데이션. `NightHeader(backgroundAsset:)`에 **경로만 넣으면 교체**된다 |
| **대화방·친구 상단** | **`[VIP]` `[★ 80]` 알약** | `HeaderPills._Pill` — 아이콘 + 글자. 프라임이 아니면 `[Prime]` 유도로 바뀐다 |
| **대화방·친구 상단** | **탭 알약** `[💬 대화 N]` | `PillTabs._TabButton` — 테두리 + 배지 |
| **[포스트 정보]·[프로필 보기]** | ⋮ 메뉴·`1/9` 알약·하단 버튼 | Material `PopupMenuButton` + 코드로 그린 알약 |
| **대화 신청·친구 신청 팝업** | 상대 한 줄·안내 상자 | 코드로 그림(⑥-B) |
| **BM 5종** | 삽화(부스트 상자·루나 보물상자) | 아이콘으로 대신 |
| 🚨 **관심사 등록 시트 · 달빛가든 카드** | **칩 37종 · 그룹 아이콘 4 · 시트 버튼** | `Icons.*` + `Container`. **전부 이미지가 정본이다**(§0-2) — `영화` 한 장만 와 있다 |
| ~~달빛가든 — 빈 상태 배경~~ | — | 🚫 **요청하지 않는다**(기획 확정 2026-09-19). 기획서에 빈 상태가 처음부터 없고, **빈 자리는 봇 데이터가 채울 예정**이다 — [09 §4](09-next-task-handoff.md). 포스트의 `back_nopost.png`에 해당하는 짝을 **일부러 안 받는다** |

> 🚨 **교체가 필요한 그림이 하나 있다 — `assets/images/login_bg.jpg`.**
> 이 그림에는 *"밤에만 만나는 대화"* 가 **구워져 있는데**, Plan_3에서 야간 게이트가
> 폐지돼 **24시간 운영**이다. 시안(img00)은 *"매일밤, 새로운 사람이 찾아오는 곳"* 이다.
> 글자가 그림 안에 있어 코드로 고칠 수 없다 — **새 그림을 받아야 한다.**
> (곁가지: ARB의 `loginTagline`은 아무 데서도 쓰지 않는다. 문구가 그림 안에 있어서다)
| 공통 | 기능 아이콘 전반 | `Icons.*` 기본 아이콘 |
| 공통 | 빈 상태 | 회색 아이콘 1개 |

> 📌 **달빛가든 필터 줄은 네 칸이다 — 네 번째는 `[달빛 한마디]` 버튼.**
> 폐지된 스포트라이트 칩 자리에 **같은 크기(334×94)로** 데일리 참여 이벤트 진입 버튼이 들어간다
> (기획서 4-1 이미지 주석: *"데일리 참여 이벤트"*). 드롭다운 칩이 아니라 **채워진 버튼**이고
> 달 아이콘이 붙어 있다. **화면이 ⑤단계에 만들어지므로 지금은 자리만 비워 두고 있다.**
>
> ⚠️ **2026-08-29 정정** — 직전까지 이 자리에 "3칩 기준으로 요청할 것"이라고 잘못 적혀 있었다.
> ①단계에서 스포트라이트를 지우며 그 칸을 빈 자리로 처리한 것이 원인이고,
> [12 §2-1](12-plan3-diff.md)에 *"달빛가든 상단의 [달빛 한마디] 버튼"* 이라고 이미 적어 둔 것과 어긋났다.
> **리소스는 네 칸 기준으로 요청할 것.**

### 🚨 이 앱의 UI 언어는 **두 종류**다 (2026-08-08 확정)

리소스를 다룰 때 이것부터 알고 시작해야 한다.

| 종류 | 무엇 | 언어 전환 |
|---|---|---|
| **폰트** | ARB(`app_ko.arb`/`app_ja.arb`)가 그리는 글자 | 기기 언어를 따라 자동으로 바뀐다 |
| **이미지** | 그림에 글자가 **구워져 있는 것**(필터 칩, 타이틀, 로그인 배경…) | **바뀌지 않는다 → 일본어판 이미지를 따로 받아 교체** |

**이미지 속 한글은 "덜 옮긴 것"이 아니라 의도된 상태다.** 폰트로 대체하려 들지 말 것.

> ✅ **2026-09-14 재확인.** *"하단 내비 라벨·필터 칩처럼 평평한 글자는 폰트로 되돌리면
> 일본어 요청이 20장에서 7장으로 줍니다"* 라고 제안했으나 **기획이 지금 방식을 유지하기로 했다** —
> **그림에 글자가 붙어 있으면 그림 자체가 언어별로 바뀐다**([14](14-ui-ground-rules.md) §2).
> **다시 제안하지 말 것.** 글자 수가 줄어드는 것보다 디자인이 그대로 가는 것이 우선이다.
일본어판은 **같은 규격의 일본어 이미지**를 받아 교체한다(같은 파일명 / `ja/` 폴더 등).
규격만 같으면 코드를 안 고쳐도 되도록 `ArtImage`가 원본 크기를 상수로 들고 있다 —
**기획에 요청할 때 "같은 규격으로"를 반드시 명시할 것.**
🚨 **단, 여러 개가 한 줄에 나란히 서는 자리(하단 주메뉴 글자)는 기준이 "제 칸 안에"다** —
거기서 "한국어와 같은 폭"을 요청하면 **글자가 더 작아진다**([14](14-ui-ground-rules.md) 요청 규칙 2).

> 📌 **일본어판 이미지 목록 작성이 ③단계의 선행 작업이다.**
> `assets/images/` 전체를 훑어 글자가 있는 파일을 추리면 된다. 달빛가든 쪽은
> `lib/features/garden/presentation/widgets/garden_art.dart`의 상수 목록이 그대로 목록이 된다.

### 📌 수령 이력

- **2026-09-06 — Plan_4 리소스 46장 수령·적용 완료**(`D:\Plan_Chat\Plan_4`).
  포스트·달빛가든 두 화면과 **하단 주메뉴**가 전부 그림으로 교체됐다.
  - ⚠️ **좌표 표기가 바뀌었다.** Plan_3의 `X*Y` 단위(×28.4)가 아니라
    **1080 캔버스 픽셀값을 그대로** 적는다(`47, 106`). `*_참고용.png`는 **옛 표기**이니
    `*_좌표값.png`만 정본으로 볼 것([07](07-work-log.md) 함정 #48).
  - **`포스트 외곽선_일반/앨범패스` 두 장은 그림으로 쓴다.** 처음엔 "얇은 선이라 코드가 낫다"고
    판단해 코드로 그렸는데, **실물로 보니 무지개 그러데이션이 코드보다 곱고** 세로 눌림도
    눈에 띄지 않았다. 그림 쪽으로 확정했다(코드용 상수는 `GardenArt`에 남겨 뒀다).
  - 🚨 **두 그림의 여백이 다르다**(알파 실측): 일반은 선이 가장자리에 붙어 있는데(여백 0),
    앨범패스는 발광이 번져 **선이 사방 6~8px 안쪽**이다. 같은 상자에 채우면 패스 쪽만
    선이 안으로 들어가 **사진이 밖으로 나온다** → 여백만큼 넓혀 그린다([07](07-work-log.md) 함정 #51).
    **외곽선류를 받으면 알파를 먼저 실측할 것.**
  - **`배경_등록 사진 없을때.png`는 일본어판이었고**, 같은 날 한국어판을 추가로 받아 해소됐다(아래).

> ✅ **`post/empty_bg.png`는 해소됐다**(2026-09-06). 한국어판을 **같은 규격(1022×1790)** 으로
> 받아 이 앱의 **첫 언어별 그림 사례**가 됐다. 규칙은 이렇게 정했다:
>
> ```
> assets/images/post/empty_bg.png      ← 한국어(원문, 기본)
> assets/images/post/ja/empty_bg.png   ← 일본어
> ```
>
> 코드는 `DesignCanvas.localizedAsset(context, 경로)` 하나로 고른다.
> **원문을 제자리에 두고 `ja/` 하위 폴더에 같은 파일명**을 넣으면 된다.
> ⚠️ 두 벌이 다 있는 그림에만 쓸 것(없는 쪽을 부르면 런타임에 못 찾는다).
> 폴더를 새로 만들면 `pubspec.yaml`에도 줄을 넣을 것(함정 #30).
>
> 🚨 **아직 남은 그림 — `login_bg.jpg`**
>
> | 구워진 글자 | 문제 |
> |---|---|
> | 한국어 *"밤에만 만나는 대화"* | 야간 게이트가 **폐지**돼 내용 자체가 틀렸다. 일본어판도 없다 |
>
> 코드로는 못 고친다. 위 규칙대로 **두 벌**을 받거나, 글자 없는 판을 받아 문구를 ARB로 얹는다.

**그 이전 이력**

- **2026-08-08 — 달빛가든 리소스(Plan_3) 수령·적용 완료.** 좌표 시안(`달빛가든 화면 이미지 좌표.png`)
  포함. 빨간 숫자는 `X*Y` 쌍이고 **1단위 ≈ 28.4px**(1080 캔버스, 요소 좌상단 기준).
  - ⚠️ 함께 온 `포스트 사진 외곽선.png`는 **쓰지 않고 지웠다.** 얇은 라운드 사각 외곽선이라
    늘리면 선 굵기·곡률이 찌그러지고 번진다 → `Border.all`로 코드로 그린다([07](07-work-log.md) 함정 #31).
    **앞으로도 얇은 선·단순 도형은 이미지로 받지 말 것.**
- 로그인·온보딩 배경 4장은 여전히 **임시본**이다(한국어 문구가 구워져 있음).

받을 때 **함께 확인할 것 2가지** (미팅 안건 형태는 [10](10-open-questions.md) §2)
1. **원본 출처·라이선스** — 아래 라이선스 항목 참고. 받는 김에 같이 물어 두면 두 번 안 묻는다.
2. **한국어판/일본어판 두 벌인지** — 글자가 구워진 리소스는 처음부터 두 벌로 요청하는 편이 싸다.

### ⚠️ 라이선스 먼저 확인할 것
남아 있는 4장은 **기획서에 딸려온 이미지**라 출처가 불명확하다. 스톡 사진이면 상업적 사용 범위가,
AI 생성이면 또 다른 조건이 걸릴 수 있다. **개발 중에는 문제없지만 스토어 출시 전에는
출처 확인 또는 교체가 필수**다. 기획자에게 원본 출처를 미리 확인해 둘 것.
→ 교체본이 오기로 했으므로 이 리스크는 해소될 전망이지만, **새로 받는 것도 출처를 확인해야 한다.**

---

## 1. 앱 내 이미지

| # | 리소스 | 용도 | 권장 규격 | 상태 |
|---|--------|------|-----------|------|
| 1 | 로그인 배경 | 로그인 화면 전체 배경 | **1080×2640**, JPG, 다크톤, **글자 없이** | ⏳ 임시본 있음(한국어 문구가 구워져 있음) |
| 2 | 온보딩 배경 ×3 | 닉네임·출생년도·성별/국가 단계 | **1080×2640**, JPG | ⏳ 임시본 있음 |
| 3 | 기본 프로필 이미지 | 사진 미등록 사용자 | 512×512, PNG(투명) | ❌ |
| 4 | 빈 상태 일러스트 ×4 | 친구 없음 · 대화 없음 · 포스트 없음 · 피드 없음 | 512×512, PNG(투명) | ❌ |
| 5 | 달(月) 심볼 | 로고·빈 상태·로딩에 반복 사용 | SVG 또는 512 PNG | ❌ |

**공통 주의**
- 앱은 **다크 테마 고정**이다. 밝은 배경 이미지는 화면과 충돌한다.
- 배경 이미지 위에 텍스트가 올라가므로 **중앙~하단부는 어둡고 단순하게**.
- 세로 전용(폰). 태블릿·가로는 현재 대상 아님.
- ⚠️ **원본 1080×2640(22:9)은 대부분의 폰보다 길다 — 아래가 잘린다.** (아래 §1-1 참고)
- ⚠️ **이미지 안에 글자를 넣지 말 것.** 지금 로그인 배경(#1)에 로고와 함께
  "밤에만 만나는 대화 / 오늘, 당신의 밤을 특별한 사람과 함께해요"가 **그림으로 구워져 있다.**
  일본어로 앱을 켜도 이 두 줄만 한국어로 남는다(2026-08-02 에뮬레이터에서 확인).
  → 최종본은 **글자 없는 배경**으로 받고, 문구는 화면 텍스트(ARB)로 얹는다.
  로고(달빛톡 워드마크)는 브랜드 자산이라 그대로 두어도 되지만, 일본어 워드마크가 필요한지는 별도 판단.

### 1-1. 배경 1080×2640 — 어디가 잘리고 어디가 가려지나

원본이 **22:9**라 흔한 폰(19.5~20:9)보다 길다. **잘리는 것**과 **시스템 UI에 가려지는 것**은
**서로 다른 층**이니 나눠서 본다. (코드 확인: `login_screen.dart` · `onboarding_scaffold.dart`)

**① 잘림 — 위가 아니라 아래만 잘린다**

```dart
Image.asset(..., fit: BoxFit.cover, alignment: Alignment.topCenter)
```
`topCenter`라 **위를 붙박아두고 남는 만큼 아래에서만** 잘라낸다(기본값 `center`가 아니다).
원본 폭 1080이 화면 폭과 같아 배율이 정확히 **1.0** — 화면 픽셀 = 이미지 픽셀이라 계산이 단순하다.

| 기기 | 잘리는 곳 |
|---|---|
| 1080×2400 (20:9) | 아래 **240px** |
| 1080×2340 (19.5:9) | 아래 **300px** |
| 1080×2160 (18:9, 구형) | 아래 **480px** |

**위쪽은 한 픽셀도 잘리지 않는다.**

**② 가려짐(세이프 에리어) — 잘림과 별개다**

배경은 `SafeArea` **밖**에 두어 화면 끝까지 그린다(콘텐츠만 SafeArea 안). 잘리진 않지만 **덮인다.**

| 구역 | 크기(1080 기준) | 무엇이 덮나 |
|---|---|---|
| 상단 | 약 **72~144px** | 상태바(시계·배터리) + **카메라 펀치홀** |
| 하단 | 약 **72px**(제스처) / **144px**(3버튼) | 내비게이션 바 |

**→ 디자이너에게 전할 것**

- **위 약 150px** — 잘리진 않지만 상태바·카메라홀에 걸린다. **로고·달 같은 주요 요소는 그 아래로.**
- **아래 약 500px** — **잘려 없어질 수 있다**(구형 18:9 기준). 여기엔 중요한 것을 두지 말 것.
- 로그인 화면은 하단에 **어두워지는 그라데이션**(`AppColors.nightScrim`)이 덮이므로
  아래쪽 디테일은 어차피 거의 보이지 않는다. **화면 상단~중앙에 승부를 걸 것.**

---

## 2. 앱 아이콘 · 스플래시

| # | 리소스 | 규격 | 비고 |
|---|--------|------|------|
| 6 | 앱 아이콘 원본 | **1024×1024 PNG(투명 없음)** | 여기서 전 해상도 자동 생성 |
| 7 | 안드로이드 적응형 아이콘 | foreground 1024×1024(투명) + 배경색 | 원형·둥근사각 등으로 잘리므로 **안전영역 66%** 안에 그릴 것 |
| 8 | 스플래시 로고 | 1024×1024 PNG(투명) + 배경색 | |
| 9 | 알림 아이콘(안드로이드) | 96×96 PNG, **흰색 실루엣 + 투명배경** | 컬러 아이콘을 넣으면 흰 사각형으로 보인다 |

> 생성은 `flutter_launcher_icons` / `flutter_native_splash` 패키지로 자동화한다(아직 미도입).
> 원본 1~2장만 주면 전 해상도는 코드로 뽑는다.

---

## 3. BM 화면용 (화면 25~30, 미착수)

| # | 리소스 | 용도 |
|---|--------|------|
| 10 | 루나(재화) 아이콘 | 잔액 표시·가격 표기 전반 |
| 11 | 프라임 멤버십 배지 | 프로필·피드 카드의 프라임 표시 |
| 12 | 부스트 아이콘 ×2 | 포스트 부스트 / 스포트라이트 |
| 13 | 패스 아이콘 ×2 | 앨범 패스 / 자동 번역 패스 |
| 14 | 루나 충전 패키지 이미지 | 100·550·1200 등 패키지별 |

> 상품 **구성과 가격은 서버 설정**(`app.store.*`)에서 오므로 이미지에 가격을 그려 넣지 말 것.
> 가격이 바뀌면 이미지까지 다시 만들어야 한다.

---

## 4. 스토어 등록용 (출시 시점)

| # | 리소스 | 규격 |
|---|--------|------|
| 15 | Play 스토어 아이콘 | 512×512 PNG |
| 16 | Play 피처 그래픽 | 1024×500 |
| 17 | Play 스크린샷 | 폰 최소 2장(1080×1920 이상), 권장 4~8장 |
| 18 | App Store 아이콘 | 1024×1024 PNG(투명·알파 없음) |
| 19 | App Store 스크린샷 | 6.7"(1290×2796) · 6.5"(1242×2688) 각 3장 이상 |
| 20 | 스토어 설명 문구 | 한국어·일본어 각각 |

> 스토어 스크린샷은 **실제 앱 화면**을 찍어 쓰므로, UI가 확정된 뒤 마지막에 만든다.
>
> ⚠️ **한·일 동시 오픈 확정(2026-08-08)** — 스크린샷(17·19)도 **일본어로 한 벌 더** 찍어야 한다.
> 앱을 일본어로 켜고 같은 화면을 다시 찍으면 되지만, **일본어 검수가 끝난 뒤**여야 의미가 있다.

---

## 5. 결정이 필요한 것

| 항목 | 선택지 |
|---|---|
| 기능 아이콘 | Material 기본 유지 vs 커스텀 아이콘셋 제작(하단 5탭 + 화면 내 아이콘 약 30종) |
| 국기 표시 | 이모지 유지(무료·간단, 기기마다 모양 다름) vs 이미지(일관됨, KR/JP 2장이면 충분) |
| 폰트 | 시스템 기본 vs 커스텀(한·일 동시 지원 필요 — Noto Sans KR/JP 등) |
| 이미지 포맷 | JPG/PNG vs WebP(용량 30~50% 절감, 안드·iOS 모두 지원) |

---

## 6. 넣는 방법 (참고)

```
assets/images/        # 앱 내 이미지 — pubspec.yaml에 폴더째 등록돼 있어 파일만 넣으면 된다
```
- `pubspec.yaml`의 `assets: - assets/images/` 는 이미 설정됨. **개별 파일 등록 불필요.**
- 앱 아이콘·스플래시는 패키지 도입 후 `flutter pub run` 한 번으로 생성.
- 사용자가 올리는 사진(프로필·포스트)은 여기가 아니라 **서버 스토리지**로 간다(02 문서 §3).
