<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moonlighttalk.server.post.mapper.PostMapper">

    <resultMap id="PostResultMap" type="com.moonlighttalk.server.post.entity.Post">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="sessionDate" column="session_date"/>
        <result property="publishedAt" column="published_at"/>
        <result property="replaceCount" column="replace_count"/>
        <result property="mainPhotoId" column="main_photo_id"/>
    </resultMap>

    <resultMap id="PostPhotoResultMap" type="com.moonlighttalk.server.post.entity.PostPhoto">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="storageKey" column="storage_key"/>
        <result property="orderIdx" column="order_idx"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByUserAndDate" resultMap="PostResultMap">
        SELECT * FROM posts WHERE user_id = #{userId} AND session_date = #{sessionDate}
    </select>

    <insert id="insertPost">
        INSERT INTO posts (id, user_id, session_date, published_at, replace_count)
        VALUES (#{id}, #{userId}, #{sessionDate}, #{publishedAt}, #{replaceCount})
    </insert>



    <!-- 스킵 재노출 판정 기준(V8). 사진 등록·삭제·한마디 수정 때 서비스가 부른다. -->
    <update id="touchContentUpdatedAt">
        UPDATE posts SET content_updated_at = NOW() WHERE id = #{postId}
    </update>

    <update id="updatePublishedAt">
        UPDATE posts SET published_at = #{publishedAt} WHERE id = #{postId}
    </update>

    <update id="updateMainPhotoId">
        UPDATE posts SET main_photo_id = #{photoId} WHERE id = #{postId}
    </update>

    <update id="incrementReplaceCount">
        UPDATE posts SET replace_count = replace_count + 1 WHERE id = #{postId}
    </update>

    <!--
        표시 순서는 **최신이 1번 슬롯**이다(Plan_3 §3-1 "신규 사진은 등록 시간순 슬롯, 1번이 최신").
        order_idx는 등록 순서대로 증가하는 값이라 내림차순이 곧 최신순이다.
        번호를 다시 매기지 않으므로 삭제가 있어도 순서가 흐트러지지 않는다.
    -->
    <select id="selectLikes" resultType="int">
        SELECT COALESCE(likes, 0) FROM post_stats
        WHERE user_id = #{userId} AND session_date = #{sessionDate}
    </select>

    <select id="selectPhotos" resultMap="PostPhotoResultMap">
        SELECT * FROM post_photos WHERE post_id = #{postId} ORDER BY order_idx DESC
    </select>

    <select id="selectPhotoById" resultMap="PostPhotoResultMap">
        SELECT * FROM post_photos WHERE id = #{photoId}
    </select>

    <insert id="insertPhoto">
        INSERT INTO post_photos (id, post_id, user_id, storage_key, order_idx, created_at)
        VALUES (#{id}, #{postId}, #{userId}, #{storageKey}, #{orderIdx}, NOW())
    </insert>

    <delete id="deletePhoto">
        DELETE FROM post_photos WHERE id = #{photoId}
    </delete>

    <select id="countPhotos" resultType="int">
        SELECT COUNT(*) FROM post_photos WHERE post_id = #{postId}
    </select>

    <select id="selectMaxOrderIdx" resultType="java.lang.Integer">
        SELECT MAX(order_idx) FROM post_photos WHERE post_id = #{postId}
    </select>

</mapper>
